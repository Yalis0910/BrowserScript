# 更新日志 (Changelog)

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/) 语义化版本规范。

---

## [1.4.0] - 2026-08-27

### 🎨 主题组合封装 JSON、自定义主题导入/导出与 Yohaku 初始默认配置
- **结构化 JSON 主题系统 (`ThemeEngine`)**：
  - 将所有界面样式与配色解耦封装为标准化 `LSM_THEME` JSON 格式（包含元数据、Tokens 字典、材质底色、边框、阴影、圆角及字体规范）；
  - **默认初始配置**：内置并将 **Yohaku (余白)** 作为默认初始化主题，同时提供 5 套精美官方预设（🌸 余白 / 🔷 经典科技蓝 / 🌑 曜石暗夜 / 🌸 春櫻粉紫 / 🍵 宇治抹茶）；
  - **动态 CSS 变量注入**：Shadow DOM 样式全面重构为 `--lsm-*` CSS 变量，切换主题 **0 延迟、0 闪烁、0 刷新** 实时热生效。
- **自定义主题导入 / 导出与流转**：
  - **📤 导出主题 (JSON)**：一键将当前主题打包导出为 `.json` 配置文件或直接复制格式化 JSON 到剪贴板；
  - **📥 导入主题 (JSON)**：支持选取本地 `.json` 主题文件或直接粘贴 JSON 字符串，自动校验 Schema、智能容错补全缺失字段并即时激活；
  - **✏️ 配色微调与新建**：内置可视化颜色选择器，支持实时微调 Accent、Paper、Header 等配色并保存为新的专属主题；
  - **🔄 一键恢复默认**：任何时候均可一键重置为 Yohaku (余白) 官方默认配置；
  - **内置删除防护**：官方预设受系统保护禁止误删，用户自定义主题支持一键删除与回退。
- **专属「🎨 主题风格设置」抽屉与扩展菜单联动**：
  - 工具栏下拉菜单与油猴/ScriptCat 扩展菜单新增 **「🎨 主题风格设置」** 快捷入口；
  - 抽屉内置当前主题色板预览（Color Swatches Palette）与多主题网格卡片选择器。

---

## [1.3.0] - 2026-08-27

### ☁️ GitHub Gist 云同步与三大核心数据同步策略升级
- **墓碑机制 (Tombstones) 彻底解决删除快照后被云端复活问题（问题 1）**：
  - 本地删除快照时自动生成带时间戳的墓碑标记（`LSM_TOMBSTONES`）；
  - 双向增量同步时自动合并两端墓碑列表，依据 `deletedAt >= updatedAt` 精准在云端与本地同步剔除已删除快照，杜绝旧快照被反向恢复；
  - 内置 30 天墓碑自动垃圾回收（GC）机制，防止元数据无限占用存储。
- **全自动双向互补增量合并（纯净无损同步）**：
  - 移除所有单向强制覆盖（强制推送/强制拉取），全面采用智能双向互补增量算法；
  - 本地与云端各自独有的快照自动双向合并互补，两端共有快照按 `updatedAt` 时间戳保留最新版本，双方数据 100% 完整保留不丢失。
- **全自动防抖与一键同步体系**：
  - **常规操作**：顶部状态栏实时指示云端连接状态（未配置/就绪/同步中/异常），工具栏新增 **【🔄 同步】** 快捷按钮（带旋转动效），支持一键双向增量同步；
  - **自动化控制**：新增 **【快照变更时自动同步】** 开关，在快照新增、改名、删除后执行 2 秒防抖自动同步；打开面板时若距离上次同步超过 5 分钟自动静默拉取。
- **专属「☁️ 云同步设置抽屉」与已有 Gist 实时搜索选择**：
  - 提供专属毛玻璃云同步配置抽屉，支持输入 GitHub Personal Access Token 并提供眼睛图标明密文切换；
  - 新增 **「📋 搜索/选择已有 Gist」** 交互弹窗：基于 GitHub API 获取账号下全部 Gist，支持描述/文件名/ID 实时模糊搜索，优先置顶推荐含有快照备份文件的 Gist，点击即一键绑定；
  - 提供 **「🚀 自动创建 Gist」** 功能，一键在 GitHub 上创建专属 Secret Gist 并自动绑定；
  - 提供 **「🔍 测试连接与权限」** 按钮，即时验证 Token 状态并展示用户名与 Gist 数量；
  - 隐藏设置抽屉内的滚动条，保持界面简洁现代，同时支持滚轮顺滑滑动。

---

## [1.2.1] - 2026-08-26

### 🧩 大体积快照分片轮播与乱序扫码聚合还原
- **超限分片二维码生成与 500ms 轮播播放器**：
  - 当快照体积超出标准单张二维码容纳上限（> 2.2 KB）时，智能判定并展示超限友好提示与 **「分片轮播生成」** 入口；
  - 数据自动分片（`LSM_CHUNK` 协议），每片控制在 1.2 KB 以内以保证极佳识读率；
  - 内置 **500ms 循环轮播播放器**，支持进度指示条、当前帧序号显示、暂停/继续切换、上一张/下一张手动翻页及退出分片模式；
- **摄像头乱序扫码与分片自动拼接还原**：
  - 摄像头实时扫码循环智能识别 `LSM_CHUNK` 分片协议；
  - 扫描到分片时**自动暂存且不关闭相机**，持续扫描直到全部分片齐全；
  - **完全支持乱序扫描**（任意先后顺序均可正确填槽）；
  - 摄像头画面上方实时展示 **分片接收进度 HUD 浮层与点阵状态指示器**（已接收分片绿色高亮打勾）；
  - 全部分片集齐后自动拼接重组为完整快照 JSON，并自动关闭相机进入恢复与导入操作流程。

---

## [1.2.0] - 2026-08-26

### 📷 二维码生成/扫码流转与全场景快照导入
- **快照转二维码展示与导出**：
  - 快照卡片新增「二维码」操作按钮，点击滑出专属二维码抽屉弹窗；
  - 引入 `QRCode.js` 动态渲染快照离线二维码，支持一键「下载图片 (PNG)」与「复制数据 (JSON)」，方便跨设备扫码传递快照凭据；
- **摄像头实时扫码解码**：
  - 工具栏与下拉菜单新增「扫码/导入」入口；
  - 基于 WebRTC `navigator.mediaDevices.getUserMedia` 调起环境摄像头，结合 `jsQR` 引擎实现视频流高帧率实时扫码与动态激光扫描动画；
  - 严格管理摄像头流生命周期（扫码成功、关闭抽屉、ESC 退出或重置时自动停止媒体流并释放设备轨道）；
- **图片二维码与 JSON 文件识别**：
  - 扫码抽屉提供「图片识别二维码」和「JSON 文件导入」两种备选渠道，支持文件选择与拖拽（Drag & Drop）直接解析；
- **解析结果双向操作**：
  - 识别成功后清晰展示快照名称、来源域名、凭据指标统计及跨域名安全警告；
  - 支持 **「🚀 立即解密并恢复当前页面」**（直接应用快照并跳转/刷新）与 **「📥 导入并保存到快照列表」**（持久化至本地存储库）；
- **滚动与快捷键体验升级**：
  - 二维码抽屉与扫码抽屉全面接入 `bindScrollLock` 移动端/PC 双重防穿透机制；
  - 支持 `Escape` 全局退出抽屉与 `Enter` 快捷提交保存。

---

## [1.1.7] - 2026-08-25

### 🔐 彻底解决跨设备/跨浏览器解密异常问题
- **重构加密密钥派生架构 (v3 协议)**：
  - 引入全局稳定派生密钥材料（`v3`），彻底解耦设备特征、UserAgent、浏览器差异以及二级域名变化，彻底消除跨设备导入或剪贴板恢复时偶发的解密失败；
  - 保留 5 级自适应回退解密管道（`v3` 跨设备通用 -> `v2` 原始域名 -> `v2` 当前域名 -> `legacy` 历史兼容），确保历史版本及跨子域名快照 100% 平滑兼容与无损恢复；
  - 修复单条导出功能中 `domain` 字段未优先读取记录自身域名的隐患。

---

## [1.1.6] - 2026-08-25

### 🚀 移动端滚动防穿透与触摸手势全方位升级
- **移动端 Touch 触摸滑动拦截机制**：
  - 封装专用的全平台滚动防护工具 `bindScrollLock`，新增移动端 `touchstart` 与 `touchmove` 手势监听与边界检测；
  - 智能计算列表与输入框当前 `scrollTop`、`scrollHeight` 与触摸滑动方向，当触达顶部或底部时精确阻止默认滚动（`e.preventDefault()`），彻底消除移动端滑动弹窗时外层宿主网页跟着联动的现象；
  - 为管理窗口注入 `touch-action: none`，为列表与抽屉配置 `-webkit-overflow-scrolling: touch` 与 `touch-action: pan-y`，保证移动端内部滚动流畅且互不干扰。
- **悬浮球与管理窗口 Touch 触摸拖拽支持**：
  - 重构 `makeDraggable` 支持同时响应鼠标事件与移动端触摸事件（`touchstart` / `touchmove` / `touchend`），手机端长按拖拽悬浮球更跟手顺滑。

---

## [1.1.5] - 2026-08-25

### ✨ 新增 Tampermonkey / 油猴菜单动态配置修改支持
- **多维度扩展菜单控制**：
  - 针对 Tampermonkey 等不支持脚本配置后台直接修改的扩展环境，全面增强 `GM_registerMenuCommand` 菜单系统，提供 4 个专属快捷配置菜单及精美毛玻璃交互弹窗：
  1. **🛡️ 过滤模式切换**：动态展示当前处于白名单还是黑名单，点击弹窗一键互转切换；
  2. **📝 编辑域名规则列表**：提供支持多行文本编辑的大号弹窗，支持通配符 `*` 并提供「+ 添加当前网站」快捷按钮；
  3. **🔒 本地数据加密设置**：动态展示当前加密状态，点击弹窗一键开启/关闭 AES-GCM 256 位加密存储；
  4. **🔄 恢复后自动刷新设置**：动态展示当前自动刷新策略，点击弹窗一键切换「直接刷新」与「不默认刷新（弹窗确认）」。

---

## [1.1.4] - 2026-08-25

### ✨ 域名过滤模式升级（黑白名单 + 域名列表）
- **灵活的黑白名单双模式**：
  - UserConfig 新增 `filter_mode` 过滤模式配置，支持 **「白名单模式（默认，仅对列表生效）」** 与 **「黑名单模式（排除列表中网站，其他全生效）」**；
  - 提取解析逻辑支持自动兼容完整的中文展示项与纯英文配置。
- **域名规则配置重构与向下兼容**：
  - 将原配置升级为 `Config.host_list`，每行一条规则，支持通配符 `*`（如 `https://*.example.org*` 或 `*.baidu.com`）；
  - 内置自动读取与平滑兼容历史版本中的 `Config.show_host` 数据。
- **扩展菜单与弹窗自适应联动**：
  - 扩展菜单、未激活拦截弹窗以及主管理弹窗均自动感知当前的黑白名单模式，动态调整「永久开启」与「永久关闭」的文案与行为（白名单模式下加入/移出白名单；黑名单模式下移出/加入黑名单）。

---

## [1.1.3] - 2026-08-25

### 🚀 核心性能与加解密跨端兼容升级
- **Cookie 批量并发处理性能跃升**：
  - 将 `clearAllData` 与 `restoreSession` 中原本串行阻塞执行的 `GM_cookie.delete` 与 `GM_cookie.set` 重构为 `Promise.all` 批量并发异步处理，恢复与清空数十个 Cookie 时耗时降低近 90%。
  - 过滤无效的 `sameSite: "unspecified"` 写入参数，规避特定浏览器内核的底层报错。
- **解耦 UA 的高兼容加解密体系**：
  - `CryptoEngine` 升级密钥派生材料为稳定版本（`LSM_KEY_V2_SNAPSHOT_${host}`），彻底消除因浏览器版本更新或跨设备/跨浏览器导出导入时的解密失败问题；
  - `decrypt` 流程内置自动回退兼容机制，无缝向下兼容历史版本中已保存的快照数据。
- **全局 Paste 快捷恢复防误触收敛**：
  - 快捷快照识别与拦截条件优化为「仅当管理窗口打开」时响应，避免在普通网页浏览、Swagger 或在线代码/JSON 编辑器中正常粘贴时被脚本误拦截。
- **UI 状态指示栏与动效修复**：
  - 点亮管理窗口顶部「Cookie 模式（全量 / 基础）与本地加密状态」指示栏；
  - 补齐宿主 DOM 关键帧淡入动画 `@keyframes lsmFadeIn`。
- **视口防溢出与全局 Hook 幂等性防护**：
  - 新增 `window.resize` 视口边界动态计算与贴边校准，防止浏览器缩小时悬浮球与管理窗口漂移出可视区域；
  - 为 SPA 路由 `history.pushState` / `replaceState` 代理增加 `__LSM_HISTORY_HOOKED__` 幂等标识，防止重复包装。

---

## [1.1.2] - 2026-08-25

### ✨ 新增图标 (logo.svg)

- 新增图标 (logo.svg) 用于扩展插件图标显示，提升用户体验。

---

## [1.1.1] - 2026-08-24

### 🚀 核心性能与数据健壮性升级
- **PBKDF2 派生密钥内存池 (Key Cache)**：
  - 在 `CryptoEngine` 内部引入域名与 Salt 级密钥缓存池，同一会话内复用 `CryptoKey`，彻底消除重复 100,000 次哈希迭代的 CPU 阻塞，恢复与加解密毫秒级响应。
- **Cookie `SameSite` 属性全量适配**：
  - 在 `restoreSession` 中显式回传 `sameSite` 属性，完美兼容现代浏览器跨站认证 Cookie（`SameSite=None/Lax/Strict`），杜绝第三方单点登录 (SSO) 凭证被浏览器静默丢弃。
- **跨浏览器全域 Cookie 彻底清空与规范化**：
  - 彻底清除带点（`.domain.com`）与不带点（`domain.com`）的多级域名 Cookie，避免跨 Chrome / Edge / Firefox 导入时并存两份同名 Cookie。
- **SPA 单页应用 (Vue/React/Angular) 路由动态感知**：
  - 挂载 `history.pushState`、`history.replaceState` 与 `hashchange` 监听，无刷新路由切换时自动更新来源 URL 与快照命名。
- **快照体积预估与指标计算**：
  - 保存抽屉实时计算快照总字节大小（KB），并在体积过大（>1.5MB）时提供轻度黄色预警与状态反馈。

---

## [1.1.0] - 2026-08-24

### ✨ 新增与重大体验升级
- **全局 Ctrl+V / Paste 智能快照捕获恢复**：
  - 全局监听键盘 `Ctrl+V`（或粘贴操作），智能识别剪贴板中的单条/批量快照 JSON 数据；
  - 检测到快照特征时自动拦截默认行为，弹出快捷恢复确认提示，点击确认即可一键解密还原并生效。
- **快照实时搜索过滤与视觉打磨**：
  - 工具栏下方集成精细化搜索框，输入框具备聚焦蓝色呼吸光晕、图标联动高亮与一键清空按钮 `✕`；
  - 完美适配手机端/小屏模式，支持按快照名称、保存时间或来源 URL 即时模糊筛选。
- **悬浮球智能贴边吸附与呼吸透明度**：
  - 悬浮球拖拽松开后自动计算最近屏幕边缘并平滑吸附贴边，彻底告别悬停在屏幕中央挡视线；
  - 空闲状态下自动保持 `0.65` 轻度透明度，鼠标悬浮时瞬间恢复 `1.0`，兼顾可用性与无干扰体验。
- **当前生效快照状态标识**：
  - 自动记录当前网站正在生效/最近恢复的快照 ID；
  - 对应卡片高亮展示翠绿 `[✓ 当前生效]` 徽章与微光高亮边框，多账号状态一目了然。
- **剪贴板一键流转与免文件快速恢复**：
  - 卡片操作栏新增「复制」按钮，一键将加密快照数据导出至系统剪贴板；
  - `⋮` 下拉菜单新增「从剪贴板恢复(免文件)」，跨浏览器/跨设备无需下载上传文件，粘贴即可秒级恢复。
- **智能去重导入机制**：
  - 导入备份数据时自动比对快照加密指纹，已存在的重复记录自动跳过并弹出清晰的状态提示。

---

## [1.0.1] - 2026-08-24

### ✨ 新增与优化
- **移动端与小屏响应式适配**：
  - 优化操作工具栏布局为紧凑单行流式排版，按钮防挤压换行（`white-space: nowrap`）；
  - 增加 `@media (max-width: 480px)` 移动端媒体查询，弹窗宽度与高度自适应手机全屏显示；
  - 记录卡片操作按钮在小屏幕下支持自适应对齐。
- **现代化视觉重构**：
  - 曜石黑蓝渐变 Header，搭配半透明毛玻璃域名胶囊（`backdrop-filter: blur`）；
  - 凭据状态彩色 Chips 徽章组（Cookie / LocalStorage / SessionStorage / 加密标识）；
  - 3 宫格凭证扫描指标预览抽屉。
- **操作与菜单整合**：
  - 工具栏增加 `⋮` 更多操作下拉菜单，整合「批量导出」、「批量导入」和「从文件恢复(不导入)」。
- **双重滚动防穿透**：
  - 添加 CSS `overscroll-behavior: contain` 与精准 JS 滚轮事件拦截，解决弹窗内滚动列表穿透影响宿主网页的问题。
- **规范与文案统一**：
  - 界面与系统文案统一升级为「快照」体系；
  - 添加 MIT 开源协议（`@license MIT`）。

---

## [1.0.0] - 2026-08-24

### 🎉 初始版本发布
- **全维度快照捕获与还原**：
  - 优先调用 `GM_cookie` API 获取全量（包含 HttpOnly）Cookie，并支持平滑降级为 `document.cookie`；
  - 支持完整的 `localStorage` 与 `sessionStorage` 键值对捕获与还原。
- **高强度本地加密**：
  - 基于 Web Crypto API 实现硬件级 AES-GCM (256-bit) + PBKDF2 动态加密，内存数据恢复后自动擦除（`wipeMemory`）。
- **无干扰交互架构**：
  - 基于 Shadow DOM 彻底隔离宿主页面 CSS 样式；
  - 悬浮球拖拽与多站点独立记忆位置；
  - 支持白名单机制（`Config.show_host`），默认不干扰未配置站点；
  - 深度集成 Tampermonkey / ScriptCat 扩展菜单命令分发。
- **备份与恢复机制**：
  - 记录来源 URL，恢复时支持智能跳转/刷新回保存页面；
  - 切换不同快照前自动彻底清空旧数据，防止状态残留与串号；
  - 支持批量 JSON 导出/导入、单条记录导出以及跨域名导入时的智能安全确认。
